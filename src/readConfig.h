#ifndef READCONFIG
#define READCONFIG

#include "cstorage.h"

/**
 * This function reads the config.txt file and fills
 * it with useful data of each image
 * @param[in] images A reference to the storage
*/
void readConfig( CStorage & images );

#endif //READCONFIG